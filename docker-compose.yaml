services:
  flask-app:
    build:
      context: .
    #ports:
    #  - 8888:8888
    volumes:
      - ./src:/app

  nginx:
    image: nginx:1.27.0-alpine
    ports:
      - 8080:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask-app